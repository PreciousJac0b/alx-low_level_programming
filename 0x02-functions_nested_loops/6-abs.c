#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute valueof an integer
 *
 * @n: an integer
 *
 * Return: The absolute value of @n
 */
int _abs(int n)
{
	return(abs(n));
}
