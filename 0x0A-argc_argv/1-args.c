#include <stdio.h>

/**
 * main - returns the length of argsc
 * @argc: number of arguments
 * @argv: array containing arguments
 *
 * Return: Always 0
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
